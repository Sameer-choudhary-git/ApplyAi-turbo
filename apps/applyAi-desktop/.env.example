# Desktop setup client configuration. These values are required at runtime or
# can be injected by the release build process through package.json extraMetadata.
API_ENDPOINT=https://api.your-domain.example/api/sessions/unstop/submit
ENCRYPTION_KEY=generate-the-same-64-character-hex-key-used-by-the-api
