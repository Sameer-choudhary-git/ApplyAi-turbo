{
  "name": "@applyai/utils",
  "version": "1.0.0",
  "description": "Shared utilities for ApplyAI",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
  ".": {
    "types": "./src/index.ts",
    "default": "./src/index.ts"
  }
},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "lint": "eslint . --ext .ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@applyai/config": "workspace:*",
    "@aws-sdk/client-s3": "^3.1026.0",
    "dotenv": "^17.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.19.17"
  }
}