{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @applyai/sentry build && pnpm --filter @applyai/web build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
